# To run the express application
  > npm run express

# To run the node server
  > npm run nodeServer

# Pages:
  - First LazyLoader:
    > http://localhost:5000/

  - Second LazyLoader:
    > http://localhost:5000/lazy